# medcheck 課題リスト

最終更新: 2026-09-03

---

## A. バグ（動作がおかしい）

- [x] **A-1. 選択日のハイライトが機能しない**（完了: 2026-09-03 / `3fe7462`）
  - `selectedDate` を `Calendar` の props に追加し、比較対象を `currentDate` から差し替えた。

- [x] **A-2. 前月・翌月のセルがクリックできてしまう**（完了: 2026-09-03 / `465dc27`）
  - クリックでその月へ移動して、その日を選択する挙動にした（`disabled` は選ばなかった）。
  - `App.handleDateSelect` が表示中の月と違う日の選択時に `currentDate` も追従させる。
  - あわせて、グレーのセルにも服用状況のドットを表示するようにした。

- [x] **A-3. 起動直後に localStorage を空で上書きする危険**（完了: 2026-09-03 / `dd6fa4f`）
  - 読み込みを `useState` の初期化関数に移し、保存は `handleRecordChange` の中でのみ
    行うようにしてマウント時の書き込みを無くした。
  - あわせて `react-hooks/set-state-in-effect` の lint エラーが解消し、
    **`npm run lint` はグリーンになった**。

- [ ] **A-4. localStorage の中身を検証していない**
  - 場所: `src/App.tsx:17`
  - `JSON.parse` の結果を無条件で `MedicationData` として扱っている。型が壊れていると描画時に落ちる。

---

## B. 仕様として詰めるべき点

- [ ] **B-1. 統計の「総記録数」が達成率になっていない**
  - 場所: `src/App.tsx:151-154`
  - 現状は *何らかの記録がある日数* を数えているだけ。分母（その月の服用予定回数）がないため「ちゃんと飲めているか」が分からない。
  - 服薬記録アプリとして本来ほしいのは達成率のはず。

- [ ] **B-2. 統計の「データ件数」が全期間の合計**
  - 場所: `src/App.tsx:157-160`
  - 「今月の統計」という見出しの中に全期間の値が混ざっている。

- [ ] **B-3. スケジュールがハードコード**
  - 場所: `src/types.ts:2-10`
  - 月曜=朝夜 / 火曜=朝のみ が固定。変更にはコード修正が必要。
  - 対応: UI から編集できるようにするか、この仕様で確定なのかを決める。

- [ ] **B-4. データのエクスポート／インポート手段がない**
  - localStorage のみのため、ブラウザのデータを消すと全消滅し、機種変更でも引き継げない。

---

## C. 後片付け（動作には影響なし）

- [ ] **C-1. 未使用ファイルの削除**
  - 参照 0 件のテンプレート残骸:
    - `src/App.css`
    - `src/assets/hero.png`
    - `src/assets/react.svg`
    - `src/assets/vite.svg`
    - `public/icons.svg`
  - ※ `public/favicon.svg` のみ `index.html` から参照あり。残すこと。

- [ ] **C-2. types.ts の未使用エクスポート**
  - `getDayName`、`keyToDate`、`TIMING_LABELS` が定義のみで `src` 内のどこからも使われていない。

- [ ] **C-3. README がテンプレートのまま**
  - Vite の雛形の内容で、medcheck について何も書かれていない。

- [ ] **C-4. `index.html` の `lang="en"`**
  - 日本語アプリなので `ja` が正しい。

---

## D. 環境

- [x] **D-1. Git リポジトリになっていない**（完了: 2026-09-03）
  - `git init` して `main` ブランチに初期コミット `08291a9` を作成した（24ファイル）。
  - `node_modules` / `dist` が `.gitignore` で除外されていることを確認済み。

- [ ] **D-2. テストが一切ない**
  - カレンダーの日付生成やスケジュール判定は単体テストの価値が高い。

- [ ] **D-3. 潜在的なタイムゾーン依存（国内利用のみなら対処不要）**
  - 場所: `src/App.tsx:122`、`src/types.ts:44`
  - `new Date("2026-09-02")` は UTC 深夜として解釈される。
  - 検証結果: **JST では正しく動く**（UTC+9 なので同日の 9:00 になる）。UTC より西のタイムゾーンでは 1 日ずれる（New York では 9/1 と判定された）。

---

## 着手順の案

`C` の一括掃除 → `B` の仕様検討

---

## 完了済み

- [x] **Tailwind v4 の設定不整合を修正し、ブラウザで表示できる状態にした**（2026-09-03）
  - `@tailwindcss/postcss` を導入し、`postcss.config.js` を更新
  - `src/index.css` を `@import "tailwindcss";` に置換し、テンプレート残骸のCSSを削除
  - v4 で不要になった `tailwind.config.js` を削除
  - `npm run build` を通すため未使用 import を削除
